{
  "name": "supervisor-assistant",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first, offline supervisory cockpit. No server, no telemetry, no runtime network dependencies.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-single-file.mjs && node scripts/verify-no-network.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "playwright": "^1.61.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
